services:
  f1-sweepstake:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: f1-sweepstake
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      - DATABASE_URL=file:/app/data/f1-sweepstake.db
      - NODE_ENV=production
      - DISCORD_WEBHOOK_URL=${DISCORD_WEBHOOK_URL}
    volumes:
      - f1-data:/app/data

  cron:
    image: alpine:3.21
    container_name: f1-sweepstake-cron
    restart: unless-stopped
    depends_on:
      - f1-sweepstake
    volumes:
      - ./docker/crontab:/etc/crontabs/root:ro
    command: crond -f -l 2

  sqlitebrowser:
    image: lscr.io/linuxserver/sqlitebrowser:latest
    container_name: f1-sweepstake-db-gui
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - PUID=0
      - PGID=0
      - TZ=Europe/London
    volumes:
      - f1-data:/data
    shm_size: "1gb"
    depends_on:
      - f1-sweepstake

volumes:
  f1-data:
